# Deploy de DRF en Vercel

### Preparando codigo para el deploy
- Crear el archivo `requirements.txt`
    ```bash
    pip freeze > requeriments.txt
    ```

- Crear el archivo `vercel.json` y configurar:
    ```python
    
    ```
